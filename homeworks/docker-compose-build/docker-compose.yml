---
version: '3'

services:
  builder:
    build: maven-app
    volumes:
      - type: volume
        source: /tmp
        target: /tmp/builddata

  runner:
    build: tomcat-app
    volumes:
      - type: volume
        source: /tmp
        target: /tmp/builddata
volumes:
  builddata:


